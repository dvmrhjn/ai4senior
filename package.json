{
  "name": "ai4seniors-wizard",
  "private": true,
  "description": "AI4Seniors Wizard — static UI + /api/chat for Cloudflare Pages or Vercel",
  "scripts": {
    "dev": "wrangler pages dev .",
    "deploy": "wrangler pages deploy . --project-name=ai4seniors-wizard"
  },
  "devDependencies": {
    "wrangler": "^4.14.0"
  }
}
